# Capture click IDs

Allowlist `gclid`, `gbraid`, `wbraid`, `fbclid`, `msclkid`, `ttclid`, and the
UTM fields required by the host. Capture before redirect or hydration. Record
field presence and source location; do not log raw values.