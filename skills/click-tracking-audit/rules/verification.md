# Verification

Report source evidence, local assertions, browser behavior, CI results, and
provider receipts as separate layers. Use `PASS`, `WARN`, `FAIL`, `NOT_RUN`, or
`UNKNOWN`; never promote simulation into delivery proof.