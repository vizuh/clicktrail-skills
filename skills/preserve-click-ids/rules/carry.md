# Carry rule

Test HTTP-to-HTTPS, trailing-slash, SPA, authentication, checkout, iframe, and
cross-apex-domain transitions. Use a signed, short-lived linker for a domain
boundary; do not put raw identity in an unsigned URL.