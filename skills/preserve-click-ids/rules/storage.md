# Storage rule

Gate persistence on the host CMP. Define first-touch write-once, last-touch
update, expiry, schema version, and denial/withdrawal behavior. Handle storage
exceptions without logging identifiers.