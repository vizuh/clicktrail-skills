# Attachment rule

Attach only an allowlisted envelope to a server-owned lead, order, or conversion
ID. Verify hidden fields after hydration and verify CRM mapping separately. Keep
provider acceptance as `UNKNOWN` until a receipt exists.